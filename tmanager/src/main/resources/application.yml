# 服务配置
server:
  port: 80
spring:
  # 1.应用配置
  application:
    name: ihrm-company #指定服务名
    # 2.数据库连接池
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:9999/tm
    username: root
    password: 123456
    # 3.JPA
  jpa:
    database: MySQL
    open-in-view: true
    show-sql: true
  thymeleaf:    cache: false
  devtools:    restart:      enabled: true